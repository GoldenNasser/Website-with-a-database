# Website-with-a-database
# PHP & MySQL Data Entry Form

A web application that captures user input via an HTML form and inserts the data into a MySQL database using PHP. 

## Project Structure
* `index.html`: The front-end user interface and styling.
* `new.php`: The back-end script handling the database connection and SQL queries.

https://nasser.free.je/
